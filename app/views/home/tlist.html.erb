<nav class="navbar navbar-inverse bg-inverse">
<ul class="nav">
  <li class="nav-item">
    <a class="nav-link" href="#">대여자 목록</a>
  </li>
  <li class="nav-item" >
    <a class="nav-link active" href="#"><%=current_user.username%></a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="/users/sign_out">로그아웃</a>
  </li>
</ul>
  </a>
</nav>
      


<div class="container" style="margin-top:50px">
    
   <h2 style="display:inline">대여자 목록</h2><h5 style="display:inline"> (품명 / 대여기간 / 대여자 / 대여날짜)</h5>
  <div style="float:right">
   
    <div style="float:right">
    <!--<a href="/home/glist" class="btn btn-primary">신청자목록</a>-->
    <!--<a href="/home/tlist" class="btn btn-primary">대여자목록</a>-->
    <!--<a href="/home/give" class="btn btn-primary">신청목록</a>-->
<form action="give" method="get" style="display:inline">
    <input type="hidden" name="uid" value="<%=current_user.username%>">
    <button type="submit" class="btn btn-secondary">신청 목록</button>
</form>
    <form action="glist" method="get" style="display:inline">
    <button type="submit" class="btn btn-secondary">신청자 목록</button>
    <input type="hidden" name="uid" value="<%=current_user.username%>">
</form>



<form action="item_manager" method="get" style="display:inline">
    <button type="submit" class="btn btn-secondary">아이템 관리</button>
    <input type="hidden" name="uid" value="<%=current_user.username%>">
</form>

<form action="take" method="get" style="display:inline">
    <input type="hidden" name="uid" value="<%=current_user.username%>">
    <button type="submit" class="btn btn-secondary">대여 목록</button>
</form>
    <a href="/home/manager" class="btn btn-secondary"  style="color:black">메인화면</a>
   </div><hr>
  
  </div><hr>
    
  
  <ul class="list-group">
    <% i = 1 %>
    <% @posts.each do |post| %>
    <li class="list-group-item justify-content-between">
      <<%= i %>>    
      <%= post.name %> / 대여기간: <%= post.day %>일 / <%= post.uid %> / <%= post.updated_at.in_time_zone("Seoul")%>
      <span class="badge badge-default">
        <form action="complete">
          <input type="hidden" name="master" value="<%=post.master%>">
          <input type="hidden" name="item_id" value="<%=post.id%>">
          <input type="hidden" name="uid" value="<%=current_user.username%>">
          <input type="submit" class="btn btn-secondary" value="반납">
        </form>
      </span>
    </li>
    
    <% i = i+1 %>
    <% end %>
  
  </ul>

</div>