<h1>본인의 아이템 대여중인 자들의 목록입니다.</h1>
  <thead>
    <tr>
      <th>품명 /    </th>
      <th>대여기간 /</th>
      <th>소유자</th>
    </tr>
  </thead>
  <br>
<% @posts.each do |post| %>
      <tr>
        <td><%= post.name %></td>
        <td><%= post.day %></td>
        <td><%= post.master %></td>
      </tr>
    <form action="complete">
        <input type="hidden" name="master" value="<%=@master%>">
        <input type="hidden" name="item_id" value="<%=post.id%>">
        <input type="hidden" name="uid" value="<%=current_user.username%>">
        <input type="submit" value="반납확인">
    </form>
      <br>
<% end %>