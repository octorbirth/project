<h1>본인의 아이템을  신청한 사람들의 목록입니다.</h1>
  <thead>
    <tr>
      <th>품명 /    </th>
      <th>대여기간 /</th>
      <th>소유자</th>
    </tr>
  </thead>
  <br>
<% @posts.each do |post| %>
      <tr>
        <td><%= post.name %></td>
        <td><%= post.day %></td>
        <td><%= post.master %></td>
      </tr>
    <form action="land">
        <input type="hidden" name="master" value="<%=post.master%>">
        <input type="hidden" name="item_id" value="<%=post.id%>">
        <input type="hidden" name="uid" value="<%=current_user.username%>">
        <input type="submit" value="대여해주기">
    </form>
      <br>
<% end %>
<a href="/home/manager">메인화면으로</a>